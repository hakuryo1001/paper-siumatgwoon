\section{Homomorphisms}

We will be dealing with simple siumatgwoons here.

\begin{definition}[Homomorphism]
    A homomorphism is a function $\phi: S \to T$ between two siumatgwoons $S$ and $T$ that preserves the composition and constitution relations, i.e.: for all $a, b \in S$,
    \begin{itemize}
        \item $\phi(a * b) = \phi(a) * \phi(b)$
        \item $a | b$ then $\phi(a) | \phi(b)$
    \end{itemize}
\end{definition}

\subsection{Completions} 

In algebra, completions come naturally. Groups are naturally and definitionally algebraically complete, so you can't say anything there. But polynomials over fields do not necessarily have solutions to them that exist in underlying field, and so there is an opportunity to discussion completion. In those cases, define a new element $\alpha$ to just by fiat be a solution to the polynomial, and then define a new field that is the smallest field that contains the original field and $\alpha$. And by this act the old field is completed in the sense that a hitherto solutionless polynomial now sees a solution in the extended field. The extended field completes the old field per that polynomial. 

In the case of siumatgwoons, completions are far more complicated, because we are dealing with two different relations, composition and constitution. Let us consider what completion in both cases would mean.

But before that, it'd be better to define what an extension is - and from that we approach the notion of completion.

\begin{definition}[Extension]
    An extension of a siumatgwoon $S$ is a siumatgwoon $S'$ that contains $S$ as a sub-siumatgwoon.
\end{definition}

% \begin{definition}[Compositional Extension]
%     Consider a siumatgwoon $S$, where for $a,b \in S$ and $a*b$ does not exist. Define $S(a*b)$ to be a siumatgwoon such that $S(a*b) = S \cup \{a*b\}$. 
% \end{definition}

\begin{definition}[Compositional Extension]
    Let $S$ be a simple siumatgwoon, and let $a, b \in S$ such that the composition $a * b$ is undefined in $S$. A compositional extension of $S$ by $a * b$, denoted $S(a * b)$, is the minimal siumatgwoon $S' \supseteq S$ satisfying:
    \begin{itemize}
        \item $S'$ contains a new element $c \notin S$ such that $c = a * b$ is defined in $S'$,
        \item The composition operation in $S'$ extends that of $S$, i.e., all compositions defined in $S$ remain unchanged,
        \item $S'$ satisfies all definitions of siumatgwoon, extended to include $c$ as the composition of $a$ and $b$.
    \end{itemize}
\end{definition}

\begin{definition}[Constitutional Extension]
    Let $S$ be a siumatgwoon, and let $b$ be an emergent object, say of $a$, i.e. $b \in \langle a \rangle$. By definition of hai elements, we know this is to say $a|b$ but there is no way to find a complete set of objects $b_1, \ldots, b_n$ such that $b = a *b_1 * \ldots * b_n$.
    In a sense, we know that $a$ constitutes $b$, but we are missing some object(s) $c$ to compose with $a$ such that $b = a * c$.
    
    Let us write $S(a|b)$ to be the siumatgwoon such that $S(a|b) = S \cup \{c\}$ where $c$ is the "missing object" that completes the composition of $a$ and $b$, i.e. $b = a * c$.

    If $a*b \in S$, then $S(a*b) = S$.
\end{definition}

\begin{definition}[Compositional Completion]
    A siumatgwoon $S'$ is a compositional completion of $S$ if for every $a, b \in S$, $a*b$ exists in $S'$.

\end{definition}

\begin{definition}[Constitutional Completion]
    A siumatgwoon $S'$ is a constitutional completion of $S$ if for every $a\in S$, for every $b\in \langle a \rangle \subseteq S$, there exists a $c\in S'$ such that $b = a * c$.
\end{definition}


Now, before we move on, let us consider our act of the compositional extension. What exactly are we doing there? In the incomplete siumatgwoon $S$, we have an "missing" object $a*b$. In the compositional extension $S(a*b)$, we forcibly conjured up the object $c = a*b$ and added it to $S$. By the Grace of God, $S(a*b)$ is also siumatgwoon. Well, given the nature of the operation, it should seem extremely intuitive, that there's only one way to do this - in other words, if there is a $c^{'}$ where $c^{'}=a*b$, $S(c^{'})$ should be the same as $S(c)$ - they should be isomorphic - there's only a change in names. 


\subsection{Subsiumatgwoons}

\begin{definition}[Subsiumatgwoon]
    A subsiumatgwoon of a siumatgwoon $S$ is a siumatgwoon $T$ such that $T \subseteq S$ and $T$ is a siumatgwoon.
\end{definition}

Examples of subsiumatgwoons are:
\begin{itemize}
    
    \item Any siumatgwoon $S$ is a subsiumatgwoon of itself.
    \item The set of atomics $A$, elementals $E$, and composites $C$, are all subsiumatgwoons of any siumatgwoon $S$.
    \item Take any atomic $a \in A$, then $\langle a \rangle$ is a subsiumatgwoon of $S$.
    
\end{itemize}






% \begin{theorem}[Uniqueness of Compositional Extensions]
%     Let $S_1$ and $S_2$ be two compositional extensions of a siumatgwoon $S$ that adjoin a new element corresponding to $a * b$. Then there exists a unique isomorphism
% \end{theorem}


% Now, let us prove that the compositional extension is unique. 

% \begin{theorem}[Universal Property of the Compositional Extension]
% Let $S$ be a siumatgwoon, and let $a, b \in S$ such that the composition $a * b$ is undefined in $S$. Let $S(a * b)$ be the compositional extension of $S$ in which a new element $c \notin S$ is adjoined such that $c = a * b$, and all other compositions and constitutions from $S$ are preserved.

% Let $\iota: S \hookrightarrow S(a*b)$ be the canonical inclusion.

% Then for any homomorphism $\phi: S \to T$ into a siumatgwoon $T$, if the composition $\phi(a) * \phi(b)$ is defined in $T$, then there exists a unique homomorphism

% $$
% \widetilde{\phi}: S(a*b) \to T
% $$

% such that:
% \begin{itemize}
% \item $\widetilde{\phi} \circ \iota = \phi$ (i.e., $\widetilde{\phi}$ extends $\phi$),
% \item $\widetilde{\phi}(a * b) = \phi(a) * \phi(b)$.
% \end{itemize}
% \end{theorem}

% \begin{proof}
% Let $c \in S(a*b)$ denote the newly adjoined element with the defining relation $c = a * b$. Let $\phi: S \to T$ be a homomorphism such that $\phi(a) * \phi(b)$ is defined in $T$.

% We define $\widetilde{\phi}: S(a*b) \to T$ by:
% \begin{itemize}
% \item $\widetilde{\phi}(x) := \phi(x)$ for all $x \in S$,
% \item $\widetilde{\phi}(c) := \phi(a) * \phi(b)$.
% \end{itemize}

% Since $S(a*b)$ is defined as the minimal siumatgwoon generated by $S \cup \{c\}$ subject only to the new relation $c = a * b$, and since $\widetilde{\phi}$ respects this relation (because $\widetilde{\phi}(c) = \phi(a) * \phi(b)$), we can uniquely extend $\phi$ to all of $S(a*b)$ by enforcing the siumatgwoon axioms.

% To check that $\widetilde{\phi}$ is a homomorphism:

% \begin{itemize}
% \item If $x, y \in S$, then $\widetilde{\phi}(x * y) = \phi(x * y) = \phi(x) * \phi(y) = \widetilde{\phi}(x) * \widetilde{\phi}(y)$.
% \item If $x = a$, $y = b$, then $x * y = c$ in $S(a*b)$, and so:
% $$
% \widetilde{\phi}(x * y) = \widetilde{\phi}(c) = \phi(a) * \phi(b) = \widetilde{\phi}(a) * \widetilde{\phi}(b).
% $$
% \item Constitution relations are preserved because no new ones are introduced except those implied by $c = a * b$, and $\phi$ already preserves all relations on $S$.
% \end{itemize}

% Uniqueness: If there were another homomorphism $\psi: S(a*b) \to T$ with $\psi \circ \iota = \phi$ and $\psi(a * b) = \phi(a) * \phi(b)$, then by minimality of $S(a*b)$ and the defining relation $c = a * b$, the image of $c$ must be $\phi(a) * \phi(b)$, and hence $\psi = \widetilde{\phi}$ on all of $S(a*b)$. So $\widetilde{\phi}$ is unique.
% \end{proof}

% \begin{corollary}[Uniqueness of Compositional Extensions]
% Let $S_1$ and $S_2$ be two compositional extensions of a siumatgwoon $S$ that adjoin a new element corresponding to $a * b$. Then there exists a unique isomorphism

% $$
% f: S_1 \xrightarrow{\sim} S_2
% $$

% such that:
% \begin{itemize}
% \item $f$ restricts to the identity on $S$,
% \item $f(a * b) = a * b$ (i.e., the newly adjoined composites correspond under the isomorphism).
% \end{itemize}
% \end{corollary}

% \begin{proof}
% By Theorem 2, both $S_1$ and $S_2$ satisfy the same universal property: any homomorphism from $S$ into a siumatgwoon where $a * b$ is defined uniquely extends to a homomorphism from $S_1$ or $S_2$. In particular, we can define a homomorphism $f: S_1 \to S_2$ that is the identity on $S$ and sends the adjoined element in $S_1$ to the adjoined element in $S_2$, both playing the role of $a * b$.

% Similarly, we get a homomorphism $g: S_2 \to S_1$ defined in the same way. By uniqueness, $f \circ g = \mathrm{id}_{S_2}$ and $g \circ f = \mathrm{id}_{S_1}$, hence $f$ is an isomorphism.
% \end{proof}
